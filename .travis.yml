language: python
python:
- '2.7'
env:
  - AWS_DEFAULT_REGION=us-east-2
install:
- pip install awscli
script:
- echo "hello 1"
deploy:
  provider: s3
  bucket: ts-test-brayest
  acl: 'private'
  on:
    repo: brayest/braytests
